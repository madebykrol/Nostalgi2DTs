{
  "name": "server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "server.ts",
  "types": "server.d.ts",
  "scripts": {
    "dev": "tsup --watch --onSuccess \"node --enable-source-maps dist/server.js\"",
    "debug": "tsup --watch --onSuccess \"node --inspect=9229 --enable-source-maps dist/server.js\"",
    "build": "tsup",
    "preview": "node dist/server.js",
    "lint": "eslint \"**/*.ts\""
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@nostalgi2d/engine": "*",
    "@nostalgi2d/planckphysics": "*",
    "@nostalgi2d/example": "*",
    "@xmldom/xmldom": "^0.8.11"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "nodemon": "^3.1.10",
    "typescript": "5.9.3",
    "tsup": "^8.0.0"
  }
}